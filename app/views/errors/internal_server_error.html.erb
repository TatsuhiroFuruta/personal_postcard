<%# 500 internal server error %>
<%# 404 not found %>
<%# <p>ご指定したページが見つかりませんでした。</p> %>
<%= render "memos/shared/logo" %>
<%# <div class="flex items-center h-full p-16 bg-white text-gray-800"> %>
<div class="flex items-center bg-white text-gray-800">
	<div class="container flex flex-col items-center justify-center px-5 mx-auto my-8">
		<%# <div class="max-w-screen-md text-center"> %>
		<div class="text-center"> 
			<h2 class="mb-8 font-extrabold text-5xl text-gray-600">500</h2>
			<h2 class="mb-8 font-extrabold text-3xl text-gray-600">Internal Server Error</h2>
			<%# <h2 class="mb-8 font-extrabold text-9xl text-gray-600">
				<span class="sr-only"></span>Page Not found 404
			</h2> %>
			<%# <p class="text-xl md:text-2xl font-semibold">サーバー内部でエラーが発生しているため、ページを表示できません。</p> %>
            <%# <p class="mt-4 mb-8 text-gray-800 text-left">サーバー内部でエラーが発生しているため、ページを表示できません。</p> %>
			<p class="mt-4 mb-8 text-gray-800 text-left">サーバー内部でエラーが発生しているため、ページを表示できません。<br/>しばらく時間を置いてからやり直してください。</p>
			<% if user_signed_in? %>
				<div class="w-52 mx-auto bg-sky-300 px-8 py-3 rounded-lg duration-100 hover:bg-blue-400">
        			<%= link_to "投稿画面一覧に戻る", memos_path %>
      			</div>
			<% else %>
				<%= link_to "ログイン画面に戻る", new_user_session_path, class: "w-52 mx-auto block rounded-lg bg-blue-500 px-8 py-3 text-sm font-semibold text-white outline-none ring-gray-300 transition duration-100 hover:bg-blue-600 focus-visible:ring active:bg-gray-600 md:text-base" %>
			<% end %>
			<%# <a rel="noopener noreferrer" href="#" class="px-8 py-3 font-semibold rounded bg-violet-400 text-gray-900">Back to homepage</a> %>
		</div>
	</div>
</div>